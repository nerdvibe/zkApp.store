query lastNews {
  getLastNews {
    title
    body
    banner
    textPreview
    slug
  }
}

query getNews($slug: String!){
  getNews(slug: $slug){
    title
    body
    banner
    textPreview
    slug
  }
}
